[package]
name = "crc32fast-lib"
version = "1.0.0"
edition = "2021"

[dependencies]
crc32fast = "1"

[profile.release]
strip = true

[build-dependencies]
cbindgen = "0.27.0"

[lib]
crate-type = ["lib", "cdylib"]
